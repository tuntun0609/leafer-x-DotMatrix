{
  "name": "leafer-x-dot-matrix-project",
  "private": true,
  "version": "0.0.0",
  "description": "Leafer X Dot Matrix Project",
  "scripts": {
    "dev": "pnpm package:build:watch & pnpm playground:dev",
    "playground:dev": "pnpm --parallel --filter playground dev",
    "playground:build": "pnpm package:build && pnpm --filter playground build",
    "package:dev": "pnpm  --parallel --filter leafer-x-dot-matrix start",
    "package:build": "pnpm --filter leafer-x-dot-matrix build",
    "package:build:watch": "pnpm --filter leafer-x-dot-matrix build:watch",
    "publish": "pnpm package:build && cd package && npm publish"
  },
  "keywords": [],
  "author": "tuntun0609 <tun.nozomi@gmail.com>",
  "license": "ISC",
  "publishConfig": {
    "directory": "package"
  }
}